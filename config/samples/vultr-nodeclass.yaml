apiVersion: karpenter.vultr.com/v1alpha1
kind: VultrNodeClass
metadata:
  name: default
spec:
  region: syd
  osID: 1743
  kubernetesVersion: v1.35
  # Must be reachable from Vultr worker instances.
  clusterEndpoint: https://kubernetes.example.com:6443
  # Optional when kube-system/kube-root-ca.crt is available.
  # caCertHash: sha256:<64-hex-character-SPKI-hash>
  sshKeyIDs: []
  vpcIDs: []
